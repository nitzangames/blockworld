{
  "name": "blockworld",
  "version": "0.5.8",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "test": "vitest run",
    "smoke": "node scripts/smoke.mjs"
  },
  "devDependencies": {
    "vitest": "^2.1.0",
    "puppeteer": "^23.0.0"
  }
}
